// Export Icon Provider + IconBase
export { default as IconProvider } from "./IconProvider"
export { default as IconBase } from "./IconBase"
import "../styles/icon-animation.css"

// Export the entire icon set
// export * from "./icons"

// Export registry for docs, metadata, search UI (optional public API)
export { registry, iconNames, iconCategories } from "./registry"
